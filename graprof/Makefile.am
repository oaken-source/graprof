
## process this file with automake to produce Makefile.in

bin_PROGRAMS = graprof

graprof_SOURCES = graprof.c graprof.h tracefile.l trace.c trace.h timeline.c timeline.h addr.c addr.h

graprof_LDADD = -lgrapes -lbfd
